@model Cüzdan_Uygulaması.BusinessLogic.DTOs.AccountDto
@{
    ViewData["Title"] = $"{Model.Name} - Hesap Detayları";
}

<style>
    :root {
        --bg: #f5f5f7;
        --card: #ffffff;
        --text: #1d1d1f;
        --muted: #6e6e73;
        --blue: #007aff;
        --green: #28a745;
        --red: #dc3545;
        --border: rgba(0, 0, 0, 0.08);
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        --radius: 16px;
    }

    .account-header {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        padding: 24px;
        margin-bottom: 24px;
    }

    .balance-card {
        text-align: center;
        padding: 20px;
        border-radius: var(--radius);
        background: linear-gradient(135deg, var(--blue), #5AC8FA);
        color: white;
        margin-bottom: 20px;
    }

    .transaction-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        margin-bottom: 12px;
    }

    .transaction-card:hover {
        transform: translateY(-1px);
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
    }

    .btn-apple {
        background: #000;
        color: white;
        border: none;
        border-radius: 12px;
        padding: 10px 20px;
        font-weight: 600;
        transition: all 0.2s ease;
    }

    .btn-apple:hover {
        background: #333;
        color: white;
        transform: translateY(-1px);
    }

    .income-indicator {
        color: var(--green);
    }

    .expense-indicator {
        color: var(--red);
    }

    .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: var(--muted);
    }

    .account-info {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        padding: 20px;
        margin-bottom: 20px;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 0;
        border-bottom: 1px solid var(--border);
    }

    .info-row:last-child {
        border-bottom: none;
    }

    .info-label {
        font-weight: 600;
        color: var(--muted);
    }

    .info-value {
        color: var(--text);
        font-weight: 500;
    }
</style>

<div class="container-fluid px-4">
    <!-- Header -->
    <div class="account-header">
        <div class="row align-items-center">
            <div class="col">
                <h2 class="mb-1">🏦 @Model.Name</h2>
                <p class="text-muted mb-0">@Model.Description</p>
            </div>
            <div class="col-auto">
                <div class="btn-group">
                    <a asp-controller="Transaction" asp-action="Create" asp-route-accountId="@Model.Id"
                        class="btn btn-apple">
                        <i class="fas fa-plus me-2"></i>Yeni İşlem
                    </a>
                    <a asp-action="EditAccount" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                        <i class="fas fa-edit me-2"></i>Düzenle
                    </a>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-2"></i>Geri Dön
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Account Info -->
        <div class="col-md-4 mb-4">
            <div class="balance-card">
                <h5 class="mb-1">Mevcut Bakiye</h5>
                <h2 class="mb-0">₺@Model.Balance.ToString("N2")</h2>
            </div>

            <div class="account-info">
                <h6 class="mb-3">Hesap Bilgileri</h6>
                <div class="info-row">
                    <span class="info-label">Hesap Adı</span>
                    <span class="info-value">@Model.Name</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Açıklama</span>
                    <span class="info-value">@(string.IsNullOrEmpty(Model.Description) ? "Belirtilmemiş" :
                                                Model.Description)</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Toplam İşlem</span>
                    <span class="info-value">@Model.Transactions.Count()</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Oluşturma Tarihi</span>
                    <span class="info-value">@Model.CreatedDate.ToString("dd MMMM yyyy")</span>
                </div>
            </div>

            <!-- Quick Stats -->
            <div class="row">
                <div class="col-6 mb-3">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body text-center py-3">
                            <div class="text-success">
                                <i class="fas fa-arrow-up fa-lg mb-1"></i>
                            </div>
                            <h6 class="text-muted mb-1">Toplam Gelir</h6>
                            <h6 class="text-success mb-0">
                                ₺@Model.Transactions.Where(t => t.Type ==
                                                                Cüzdan_Uygulaması.Models.TransactionType.Income).Sum(t => t.Amount).ToString("N2")
                            </h6>
                        </div>
                    </div>
                </div>
                <div class="col-6 mb-3">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body text-center py-3">
                            <div class="text-danger">
                                <i class="fas fa-arrow-down fa-lg mb-1"></i>
                            </div>
                            <h6 class="text-muted mb-1">Toplam Gider</h6>
                            <h6 class="text-danger mb-0">
                                ₺@Model.Transactions.Where(t => t.Type ==
                                                                Cüzdan_Uygulaması.Models.TransactionType.Expense).Sum(t => t.Amount).ToString("N2")
                            </h6>
                        </div </div>
                    </div>
                </div>
            </div>

            <!-- Transactions -->
            <div class="col-md-8">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="mb-0">Son İşlemler</h5>
                    <a asp-controller="Transaction" asp-action="Index" class="btn btn-sm btn-outline-primary">
                        Tümünü Gör
                    </a>
                </div>

                @if (Model.Transactions.Any())
                {
                    @foreach (var transaction in Model.Transactions.OrderByDescending(t => t.TransactionDate).Take(10))
                    {
                        <div class="transaction-card">
                            <div class="card-body py-3">
                                <div class="row align-items-center">
                                    <div class="col-auto">
                                        <div
                                            class="@(transaction.Type == Cüzdan_Uygulaması.Models.TransactionType.Income ? "income-indicator" : "expense-indicator")">
                                            <i
                                                class="fas @(transaction.Type == Cüzdan_Uygulaması.Models.TransactionType.Income ? "fa-arrow-up" : "fa-arrow-down") fa-lg"></i>
                                        </div>
                                    </div>
                                    <div class="col">
                                        <h6 class="mb-1">@transaction.Description</h6>
                                        <p class="text-muted small mb-0">
                                            @transaction.TransactionDate.ToString("dd MMM yyyy, HH:mm")
                                            @if (transaction.Category != null)
                                            {
                                                <span class="badge bg-light text-dark ms-2">@transaction.Category.Name</span>
                                            }
                                            @if (transaction.IsRecurring)
                                            {
                                                <span class="badge bg-info text-white ms-1">
                                                    <i class="fas fa-sync-alt me-1"></i>Otomatik
                                                </span>
                                            }
                                    </p>
                                </div>
                                <div class="col-auto text-end">
                                    <h6
                                        class="mb-0 @(transaction.Type == Cüzdan_Uygulaması.Models.TransactionType.Income ? "text-success" : "text-danger")">
                                        @(transaction.Type == Cüzdan_Uygulaması.Models.TransactionType.Income ? "+" :
                                                                            "-")₺@transaction.Amount.ToString("N2")
                                        </h6>
                                        <div class="btn-group">
                                            <a asp-controller="Transaction" asp-action="Details" asp-route-id="@transaction.Id"
                                                class="btn btn-sm btn-outline-primary">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a asp-controller="Transaction" asp-action="Edit" asp-route-id="@transaction.Id"
                                                class="btn btn-sm btn-outline-warning">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                }
                else
                {
                    <div class="card border-0 shadow-sm">
                        <div class="card-body empty-state">
                            <i class="fas fa-receipt fa-3x mb-3"></i>
                            <h5>Henüz işlem bulunmuyor</h5>
                            <p>Bu hesapta henüz hiç işlem yapılmamış.</p>
                            <a asp-controller="Transaction" asp-action="Create" asp-route-accountId="@Model.Id"
                                class="btn btn-apple">
                                <i class="fas fa-plus me-2"></i>İlk İşlemi Ekle
                            </a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

@section Scripts {
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script>
            // Success/Error messages
            @if (TempData["Success"] != null)
                {
                    <text>
                        Swal.fire({
                            icon: 'success',
                        title: 'Başarılı!',
                        text: '@Html.Raw(TempData["Success"])',
                        timer: 3000,
                        showConfirmButton: false
                            });
                    </text>
            }

                @if (TempData["Error"] != null)
                {
                    <text>
                        Swal.fire({
                            icon: 'error',
                        title: 'Hata!',
                        text: '@Html.Raw(TempData["Error"])'
                            });
                    </text>
            }
        </script>
}