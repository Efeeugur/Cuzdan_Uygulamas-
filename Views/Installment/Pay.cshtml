@model Cüzdan_Uygulaması.BusinessLogic.DTOs.PayInstallmentDto
@{
    ViewData["Title"] = "Taksit Ödemesi";
    var installment = ViewBag.Installment as Cüzdan_Uygulaması.BusinessLogic.DTOs.InstallmentDto;
    var accounts = ViewBag.Accounts as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
}

<style>
    :root {
        --bg: #f5f5f7;
        --card: #ffffff;
        --text: #1d1d1f;
        --muted: #6e6e73;
        --blue: #007aff;
        --green: #28a745;
        --red: #dc3545;
        --orange: #ff9500;
        --border: rgba(0, 0, 0, 0.08);
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        --radius: 16px;
    }

    .payment-container {
        max-width: 600px;
        margin: 2rem auto;
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        overflow: hidden;
    }

    .payment-header {
        padding: 2rem;
        text-align: center;
        background: linear-gradient(135deg, var(--green), #4CAF50);
        color: white;
    }

    .payment-body {
        padding: 2rem;
    }

    .installment-info {
        background: #f8f9fa;
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 0;
        border-bottom: 1px solid var(--border);
    }

    .info-row:last-child {
        border-bottom: none;
    }

    .info-label {
        font-weight: 600;
        color: var(--muted);
    }

    .info-value {
        font-weight: 600;
        color: var(--text);
    }

    .payment-amount {
        text-align: center;
        padding: 2rem;
        background: linear-gradient(135deg, #e3f2fd, #bbdefb);
        border-radius: 12px;
        margin-bottom: 2rem;
    }

    .amount-display {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--blue);
        margin-bottom: 0.5rem;
    }

    .form-label {
        font-weight: 600;
        font-size: 0.95rem;
        margin-bottom: 0.5rem;
        color: var(--text);
    }

    .form-control {
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 12px 16px;
        font-size: 1rem;
        transition: all 0.2s ease;
        background: var(--card);
        color: var(--text);
    }

    .form-control:focus {
        outline: none;
        border-color: var(--blue);
        box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.1);
        background: var(--card);
    }

    .btn-apple {
        background: #000;
        color: white;
        border: none;
        border-radius: 12px;
        padding: 12px 24px;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.2s ease;
        width: 100%;
    }

    .btn-apple:hover {
        background: #333;
        color: white;
        transform: translateY(-1px);
    }

    .btn-success-custom {
        background: var(--green);
        color: white;
        border: none;
        border-radius: 12px;
        padding: 12px 24px;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.2s ease;
        width: 100%;
    }

    .btn-success-custom:hover {
        background: #218838;
        color: white;
        transform: translateY(-1px);
    }

    .btn-secondary-custom {
        background: transparent;
        color: var(--muted);
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 12px 24px;
        font-weight: 600;
        transition: all 0.2s ease;
        text-decoration: none;
    }

    .btn-secondary-custom:hover {
        background: var(--border);
        color: var(--text);
        text-decoration: none;
    }

    .text-danger {
        color: #ff3b30 !important;
        font-size: 0.9rem;
        margin-top: 0.25rem;
    }

    .form-help {
        font-size: 0.9rem;
        color: var(--muted);
        margin-top: 0.25rem;
    }

    .form-icon {
        color: var(--blue);
        margin-right: 0.5rem;
    }

    .progress-section {
        background: #fff3cd;
        border: 1px solid #ffecb5;
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .progress-bar-custom {
        height: 8px;
        background-color: #e9ecef;
        border-radius: 4px;
        overflow: hidden;
        margin: 1rem 0;
    }

    .progress-fill {
        height: 100%;
        background: linear-gradient(90deg, var(--blue), var(--green));
        transition: width 0.3s ease;
    }

    .overdue-warning {
        background: #ffebee;
        border: 1px solid #ffcdd2;
        border-radius: 12px;
        padding: 1rem;
        margin-bottom: 1.5rem;
        color: #d32f2f;
    }
</style>

<div class="container-fluid px-4">
    <div class="payment-container">
        <div class="payment-header">
            <h2 class="mb-2">
                <i class="bi bi-credit-card me-2"></i>
                Taksit Ödemesi
            </h2>
            <p class="mb-0 opacity-75">@(installment?.Description ?? "")</p>
        </div>

        <div class="payment-body">
            <!-- Overdue Warning -->
            @if (installment?.NextPaymentDate < DateTime.Now)
            {
                <div class="overdue-warning">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <div>
                            <strong>Gecikmiş Ödeme!</strong>
                            <div class="small">Bu taksit @(installment?.NextPaymentDate.ToString("dd MMMM yyyy")) tarihinde ödenmeliydi.</div>
                        </div>
                    </div>
                </div>
            }

            <!-- Progress Section -->
            <div class="progress-section">
                <h6 class="mb-3">📊 Ödeme Durumu</h6>
                @{
                    var progressPercentage = (installment?.TotalInstallments ?? 0) > 0 
                        ? ((double)((installment?.TotalInstallments ?? 0) - (installment?.RemainingInstallments ?? 0)) / (installment?.TotalInstallments ?? 1)) * 100 
                        : 0;
                }
                <div class="progress-bar-custom">
                    <div class="progress-fill" style="width: @(progressPercentage.ToString("F1"))%"></div>
                </div>
                <div class="d-flex justify-content-between">
                    <small>%@(progressPercentage.ToString("F1")) tamamlandı</small>
                    <small>@(installment?.RemainingInstallments ?? 0) / @(installment?.TotalInstallments ?? 0) taksit kaldı</small>
                </div>
            </div>

            <!-- Payment Amount -->
            <div class="payment-amount">
                <div class="amount-display">₺@(installment?.MonthlyPayment.ToString("N2") ?? "0.00")</div>
                <p class="mb-0 text-muted">Ödenecek Tutar</p>
            </div>

            <!-- Installment Info -->
            <div class="installment-info">
                <h6 class="mb-3">Taksit Bilgileri</h6>
                
                <div class="info-row">
                    <span class="info-label">Alışveriş:</span>
                    <span class="info-value">@(installment?.Description ?? "")</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Toplam Tutar:</span>
                    <span class="info-value">₺@(installment?.TotalAmount.ToString("N2") ?? "0.00")</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Aylık Ödeme:</span>
                    <span class="info-value">₺@(installment?.MonthlyPayment.ToString("N2") ?? "0.00")</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Ödenen Tutar:</span>
                    <span class="info-value text-success">₺@(installment?.TotalPaid.ToString("N2") ?? "0.00")</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Kalan Tutar:</span>
                    <span class="info-value text-danger">₺@(installment?.RemainingAmount.ToString("N2") ?? "0.00")</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Sonraki Ödeme Tarihi:</span>
                    <span class="info-value">@(installment?.NextPaymentDate.ToString("dd MMMM yyyy") ?? "")</span>
                </div>

                @if (installment?.Category != null)
                {
                    <div class="info-row">
                        <span class="info-label">Kategori:</span>
                        <span class="info-value">
                            <span class="badge bg-light text-dark">@(installment?.Category?.Name ?? "")</span>
                        </span>
                    </div>
                }
            </div>

            <form asp-action="Pay" method="post" novalidate>
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                
                @Html.AntiForgeryToken()
                <input asp-for="InstallmentId" type="hidden" />

                <div class="mb-4">
                    <label asp-for="AccountId" class="form-label">
                        <i class="bi bi-building form-icon"></i>Ödeme Hesabı *
                    </label>
                    <select asp-for="AccountId" asp-items="accounts" class="form-control">
                        <option value="">Ödeme yapılacak hesabı seçin...</option>
                    </select>
                    <span asp-validation-for="AccountId" class="text-danger"></span>
                    <div class="form-help">Ödemenin çekileceği hesap</div>
                </div>

                <div class="mb-4">
                    <label asp-for="PaymentDate" class="form-label">
                        <i class="bi bi-calendar form-icon"></i>Ödeme Tarihi *
                    </label>
                    <input asp-for="PaymentDate" class="form-control" type="date" />
                    <span asp-validation-for="PaymentDate" class="text-danger"></span>
                    <div class="form-help">Ödemenin yapıldığı gerçek tarih</div>
                </div>

                <div class="mb-4">
                    <label asp-for="Notes" class="form-label">
                        <i class="bi bi-sticky form-icon"></i>Notlar
                    </label>
                    <textarea asp-for="Notes" class="form-control" rows="3" 
                              placeholder="Bu ödeme hakkında ek notlarınız (isteğe bağlı)..."></textarea>
                    <span asp-validation-for="Notes" class="text-danger"></span>
                    <div class="form-help">Ödeme hakkında hatırlamak istediğiniz bilgiler</div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-2">
                        <a asp-action="Details" asp-route-id="@(installment?.Id ?? 0)" class="btn btn-secondary-custom w-100">
                            <i class="bi bi-arrow-left me-2"></i>Geri Dön
                        </a>
                    </div>
                    <div class="col-md-6">
                        <button type="submit" class="btn-success-custom">
                            <i class="bi bi-check me-2"></i>Ödemeyi Tamamla
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Focus on account selection
            document.querySelector('select[name="AccountId"]').focus();
        });
    </script>
}